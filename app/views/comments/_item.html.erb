<tr>
  <td><%= comment.text %></td>
  <td class="d-flex justify-content-end">
    <%= link_to 'Edit', edit_ticket_comment_path(ticket, comment), class: 'btn btn-outline-dark py-1' %>
    <%= link_to 'Delete', ticket_comment_path(ticket, comment), method: :delete, data: { confirm: 'Are you sure you want to delete this comment?' },
                class: 'btn btn-outline-danger py-1 ml-2' %>
  </td>
</tr>